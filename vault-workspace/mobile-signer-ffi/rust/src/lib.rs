//! UniFFI-exported bindings over `vault-core` for the borrower's mobile app
//! (Kotlin/Swift). See `docs/00-capstone-brief.md` §3.3 and
//! `docs/design-notes.md` for the screens this backs.

// TODO: define the UniFFI interface (likely via a `.udl` file or the
// proc-macro API, depending on the uniffi version the team settles on) once
// vault-core's PSBT/derivation interface stabilizes.
