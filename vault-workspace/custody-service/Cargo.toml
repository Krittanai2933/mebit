[package]
name = "custody-service"
version = "0.1.0"
edition = "2024"

[dependencies]
axum = "0.8.9"
serde = { version = "1.0.229", features = ["derive"] }
serde_json = "1.0.151"
tokio = { version = "1.53.1", features = ["full"] }
vault-core = { path = "../vault-core" }
# sqlx = { version = "0.8", features = ["postgres", "runtime-tokio"] }  # replace the in-memory store once persistence is needed
